# 🚀 دليل البدء السريع

دليل مختصر لتشغيل الاختبارات وتوليد البيانات الإحصائية بسرعة.

---

## خطوة 1: التثبيت

```bash
# انتقل إلى مجلد الاختبارات
cd tests

# ثبت المتطلبات
pip install -r requirements.txt
```

---

## خطوة 2: تشغيل الخادم

```bash
# في terminal منفصل، شغل خادم RAG API
cd ..
python rag_api_server_with_video_analysis.py
```

انتظر حتى تظهر رسالة:
```
✨ Features:
  - Document RAG with unified search
  - Video RAG: Chat with video content
  ...
```

---

## خطوة 3: تشغيل الاختبارات

### الخيار 1: تشغيل جميع الاختبارات (موصى به)

```bash
python run_all_tests.py
```

### الخيار 2: تشغيل اختبارات محددة

```bash
# البحث الدلالي فقط
python test_search_performance.py

# تحليل الفيديو فقط
python test_video_analysis.py

# استخراج الكتّاب فقط
python test_writer_extraction.py

# مقاييس الأداء فقط
python test_performance_metrics.py
```

---

## خطوة 4: توليد التقارير

```bash
python generate_report.py
```

---

## 📊 العثور على النتائج

بعد تشغيل الاختبارات:

### النتائج الخام (JSON)
```
test_results/
├── search_performance_results.json
├── video_analysis_results.json
├── writer_extraction_results.json
└── performance_metrics.json
```

### التقارير النهائية
```
reports/
├── full_report_YYYYMMDD_HHMMSS.md    # تقرير Markdown كامل
├── latex_tables.tex                   # جداول LaTeX للبحث العلمي
└── summary.json                       # ملخص JSON
```

---

## 📋 استخدام النتائج في البحث

### 1. نسخ جداول LaTeX

```bash
cat reports/latex_tables.tex
```

ثم انسخ الجداول إلى ملف LaTeX الخاص بك:

```latex
\documentclass{article}
\usepackage{booktabs}

\begin{document}

\section{النتائج}

\input{reports/latex_tables.tex}

\end{document}
```

### 2. عرض التقرير Markdown

```bash
# على Linux/Mac
cat reports/full_report_*.md

# أو افتحه في محرر نصوص
```

### 3. تحليل البيانات بـ Python

```python
import json

# قراءة النتائج
with open('test_results/search_performance_results.json', 'r') as f:
    data = json.load(f)

# طباعة F1-Score عند k=5
print(f"F1@5: {data['avg_f1_at_k']['5']:.3f}")
```

---

## ⚡ نصائح سريعة

### تخطي اختبارات معينة

```bash
# تخطي اختبار الفيديو (يستغرق وقتاً)
python run_all_tests.py --skip video

# تخطي عدة اختبارات
python run_all_tests.py --skip video performance
```

### اختبار على عينة صغيرة

عدّل في ملف الاختبار:

```python
# test_search_performance.py
# بدلاً من تحميل جميع الاستعلامات
self.test_queries = self.test_queries[:5]  # أول 5 فقط
```

---

## 🐛 مشاكل شائعة وحلولها

### المشكلة 1: "Connection refused"
**السبب:** الخادم غير مشغل
**الحل:**
```bash
python rag_api_server_with_video_analysis.py
```

### المشكلة 2: "No test data found"
**السبب:** ملفات البيانات فارغة
**الحل:** البرنامج سيستخدم بيانات نموذجية تلقائيًا، لكن النتائج ستكون محدودة

### المشكلة 3: الاختبارات بطيئة جدًا
**الحل:**
```bash
# تخطي الاختبارات البطيئة
python run_all_tests.py --skip video performance
```

---

## 📈 مثال: سير عمل كامل

```bash
# 1. التثبيت
pip install -r requirements.txt

# 2. تشغيل الخادم (terminal منفصل)
python ../rag_api_server_with_video_analysis.py &

# 3. انتظر 30 ثانية لتحميل النماذج
sleep 30

# 4. تشغيل جميع الاختبارات
python run_all_tests.py

# 5. عرض النتائج
cat reports/full_report_*.md

# 6. نسخ جداول LaTeX
cat reports/latex_tables.tex > ~/my_paper/results_tables.tex
```

---

## 🎯 الجداول المطلوبة للبحث

بعد تشغيل الاختبارات، ستحصل على هذه الجداول:

✅ **جدول 1:** مقاييس البحث الدلالي (Precision, Recall, F1 @ k=1,3,5,10)
✅ **جدول 2:** دقة تحليل الفيديو (WER, CER, ROUGE)
✅ **جدول 3:** أداء استخراج الكتّاب (Precision, Recall, F1)
✅ **جدول 4:** مقاييس الأداء (زمن المعالجة، قابلية التوسع)

---

## 💡 خطوات تالية

1. **املأ بيانات الاختبار:**
   - `test_data/search_queries.json` - أضف استعلامات حقيقية
   - `test_data/video_test_cases.json` - أضف نصوص مرجعية
   - `test_data/writer_test_documents.json` - أضف أسماء الكتّاب الصحيحة

2. **شغل الاختبارات مجددًا:**
   ```bash
   python run_all_tests.py
   ```

3. **احصل على نتائج دقيقة** لبحثك العلمي!

---

**وقت التشغيل المتوقع:**
- الاختبارات الأساسية: 5-10 دقائق
- مع الفيديو: 20-30 دقيقة
- جميع الاختبارات: 30-45 دقيقة

**حجم المخرجات:**
- ملفات JSON: ~2-5 MB
- تقرير Markdown: ~50 KB
- جداول LaTeX: ~10 KB

---

✅ **جاهز!** الآن لديك جميع البيانات الإحصائية اللازمة لبحثك العلمي.
